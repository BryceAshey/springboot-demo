pipeline {
    agent {
        kubernetes {
            defaultContainer 'jenkins-mvn'
        }
    }
    stages {
        stage('Build') { 
            steps {
                sh 'mvn -B -DskipTests clean package' 
            }
        }
    }
}